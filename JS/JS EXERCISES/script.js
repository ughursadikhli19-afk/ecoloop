//1
 function PrintNum(arr) {
  arr.forEach(element => console.log(element));
}


//2
"XXXX"

//3
function MergeAndSort(arr1, arr2) {
  const  concat= arr1.concat(arr2);
  return concat.sort((a, b) => a.age - b.age);
}